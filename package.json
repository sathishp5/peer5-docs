{
  "name": "peer5-docs",
  "version": "0.0.3",
  "description": "A documentation repo using mkdocs theme with netlify",
  "scripts": {
    "watch": "mkdocs serve",
    "build": "mkdocs build --clean",
    "setup-env": "sudo apt-get install -y pip && sudo pip install mkdocs"
  }
}
